% Lê a bibliografia presente no arquivo .bib para que seja possível produzir as referências.
\addbibresource{./res/bib/bibliography.bib}

% Numerar títulos de seções até \subsubsection's
\setcounter{secnumdepth}{3}

% Imprime, na tabela de conteúdos, títulos de seções até \subsubsection's
\setcounter{tocdepth}{3}

% space between paragraphs
\setlength{\parskip}{0pt} 

\onehalfspacing % line spacing

% magic code to insert new line between header and first itemm
\def\enumfix{%
\if@inlabel
 \noindent \par\nobreak\vskip-\topsep\hrule\@height\z@
\fi}
\let\olditemize\itemize
\def\itemize{\enumfix\olditemize}

% change itemize bullet character
% \renewcommand{\labelitemi}{--}
% \renewcommand{\labelitemii}{-}

% spacing for lists
% \renewcommand{\@listI}{%
% \leftmargin=0pt
% \rightmargin=0pt
% \labelsep=5pt
% \labelwidth=20pt
% \itemindent=0pt
% %\listparindent=\parindent %indentation at the beginning of paragraph
% \topsep=0pt plus 2pt minus 4pt
% \partopsep=0pt plus 1pt minus 1pt
% \parsep=0pt plus 1pt
% \itemsep=0pt}

% change caption label formatting
\captionsetup[table]{
  labelfont=bf, %
  labelsep=period, %
  justification=raggedright, %
  singlelinecheck=false%    Necessário para alinhar à esquerda mesmo quando o texto é pequeno o suficiente para não preencher uma linha
}
\captionsetup[figure]{labelfont=bf, labelsep=period}

% standard label config for enumerate
\setenumerate{label=\roman*.,leftmargin=*,align=left}
%\setlist[itemize]{leftmargin=*}

%%%%%%%%% custom environments %%%%%%%%%%%%%%%%%%%%%%%%%%%%

\NewEnviron{nofleqn}[1]{\@fleqnfalse\begin{#1}\BODY\end{#1}}

% theorem environments -----------------------------------------------------------------------------------



